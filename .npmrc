# cross-env sets OPENSSL_CONF in each npm script (see package.json).
# openssl.cnf lowers the security level to SECLEVEL=1 so Node 22+ can
# complete TLS handshakes with MongoDB Atlas (OpenSSL 3.x regression).
